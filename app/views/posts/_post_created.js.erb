var postModal = $('#new-post-modal');
postModal.modal('hide');
postModal.find('form').find('input[type=text], input[type=password], input[type=number], input[type=email], textarea').val('');
alert('пост добавлен!')